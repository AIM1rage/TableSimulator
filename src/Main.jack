class Main {
    function void main() { 
        var Game game;
        var char key;
        do Levels.init();
        while (true) {
            let game = Game.new();
            do Display.printMenu();
            while (~(key = 0)) { let key = Keyboard.keyPressed(); }
            while (key = 0)    { let key = Keyboard.keyPressed(); }
            while (~(key = 0)) { let key = Keyboard.keyPressed(); }
            do game.run();
        }
        return;
    }
}
