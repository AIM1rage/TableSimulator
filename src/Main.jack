class Main {
    function void main() { 
        var Game game;
        var char key;
        var int levelIndex;
        let levelIndex = 2;
        do Levels.init();
        let game = Game.new();
        while (true) {
            do Display.printMenu();
            while (~(key = 0)) { let key = Keyboard.keyPressed(); }
            while (key = 0)    { let key = Keyboard.keyPressed(); }
            while (~(key = 0)) { let key = Keyboard.keyPressed(); }
            do game.run(levelIndex);
            do game.dispose();
        }
        return;
    }
}
