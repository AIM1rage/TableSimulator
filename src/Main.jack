class Main {
    function void main() { 
        var TableGame game;
        var char key;
        let game = TableGame.new();
        do Display.printMenu();
        while (~(key = 0)) { let key = Keyboard.keyPressed(); }
	    while (key = 0)    { let key = Keyboard.keyPressed(); }
        while (~(key = 0)) { let key = Keyboard.keyPressed(); }
        do game.run();
        return;
    }
}
